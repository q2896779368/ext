{
  "name": "gdf-ui",
  "version": "0.2.2-Alpha.0",
  "description": "```\r // 刷新（重命名了类，移动了类文件的位置或者其它行为此命令最快）\r sencha app refresh",
  "main": "app.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@192.168.0.233:cosmosource/gdf-ui.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cz-conventional-changelog": "^3.0.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
